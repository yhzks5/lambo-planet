server {
        listen 1000 default;
        server_name  localhost;
	location / {
		root   F:/GitHub/lambo/lambo-ui/;
		index  index.html index.htm;
		add_header Access-Control-Allow-Origin *;
	}
	access_log  logs/lambo-ui.access.log;
}
